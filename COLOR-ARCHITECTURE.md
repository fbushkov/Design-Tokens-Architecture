# Color Architecture

## Обзор

Трёхуровневая архитектура токенов цвета для дизайн-системы.

```
┌─────────────────────────────────────────────────────────────────┐
│                      PRIMITIVES                                  │
│              (базовые значения, без режимов)                     │
├─────────────────────────────────────────────────────────────────┤
│  System Colors    │  Base Colors      │  Brand Colors           │
│  ─────────────    │  ───────────      │  ────────────           │
│  neutral-25..950  │  white            │  brand-25..950          │
│  success-25..950  │  black            │  accent-25..950         │
│  warning-25..950  │  transparent-light│                         │
│  error-25..950    │  transparent-dark │                         │
│  info-25..950     │                   │                         │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        TOKENS                                    │
│         (семантические токены, light/dark режимы)                │
├─────────────────────────────────────────────────────────────────┤
│  bg/page/primary          → Primitives/neutral-25               │
│  bg/card/primary          → Primitives/neutral-25               │
│  bg/overlay/scrim         → Primitives/neutral-900              │
│  text/primary             → Primitives/neutral-900              │
│  text/brand               → Primitives/brand-500                │
│  border/default           → Primitives/neutral-200              │
│  action/primary           → Primitives/brand-500                │
│  overlay/scrim/default    → Primitives/neutral-900              │
│  ...                                                            │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      COMPONENTS                                  │
│           (компонентные токены, без режимов)                     │
├─────────────────────────────────────────────────────────────────┤
│  button/primary/bg        → Tokens/action/primary               │
│  button/primary/text      → Tokens/text/inverse/primary         │
│  input/bg                 → Tokens/bg/input/default             │
│  card/bg                  → Tokens/bg/card/primary              │
│  modal/overlay            → Tokens/overlay/scrim/default        │
│  ...                                                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 1. Primitives (Примитивы)

Базовые цветовые значения. **Без режимов** - одинаковые во всех темах.

### 1.1 System Colors (Системные цвета)
Заблокированы по умолчанию. Палитра из 19 оттенков (25-950).

| Цвет    | Base HEX  | Описание                    |
|---------|-----------|----------------------------|
| neutral | #999999   | Нейтральные серые          |
| success | #4DB35D   | Успех, подтверждение       |
| warning | #D7B923   | Предупреждение             |
| error   | #E9033A   | Ошибка, опасность          |
| info    | #3B82F6   | Информация                 |

### 1.2 Base Colors (Базовые цвета)
Фундаментальные цвета без палитры оттенков.

| Цвет              | Значение          | Описание                |
|-------------------|-------------------|-------------------------|
| white             | #FFFFFF           | Чистый белый            |
| black             | #000000           | Чистый чёрный           |
| transparent-light | rgba(0,0,0,0.3)   | Прозрачный светлый 30%  |
| transparent-dark  | rgba(0,0,0,0.7)   | Прозрачный тёмный 70%   |

### 1.3 Brand Colors (Брендовые цвета)
Редактируемые. Палитра из 19 оттенков (25-950).

| Цвет   | Base HEX  | Описание                    |
|--------|-----------|----------------------------|
| brand  | #2781F3   | Основной брендовый цвет    |
| accent | #844DBB   | Акцентный цвет             |

### Шкала оттенков
```
25 → 50 → 100 → 200 → 300 → 400 → 500 → 600 → 700 → 800 → 900 → 950
     ←── светлее ──┤     BASE     ├── темнее ──→
```

---

## 2. Tokens (Семантические токены)

Семантическое назначение цветов. **Режимы: light / dark** (+ темы).

### Категории токенов

| Категория   | Описание                           | Примеры                        |
|-------------|------------------------------------|---------------------------------|
| bg          | Фоны                               | page, card, modal, overlay     |
| text        | Текст                              | primary, secondary, brand      |
| border      | Обводки                            | default, subtle, strong        |
| icon        | Иконки                             | primary, secondary, brand      |
| action      | Интерактивные элементы             | primary, secondary, danger     |
| divider     | Разделители                        | default, subtle, strong        |
| surface     | Поверхности (legacy)               | page, card, elevated           |
| content     | Контент (legacy)                   | primary, secondary, tertiary   |
| stroke      | Обводки (legacy)                   | default, subtle, focus         |
| feedback    | Статусы                            | success, warning, error, info  |
| focus       | Фокус                              | ring, outline                  |
| status      | Статус-индикаторы                  | online, offline, pending       |
| badge       | Бейджи                             | neutral, brand, success        |
| data-viz    | Визуализация данных                | categorical, sequential        |
| nav         | Навигация                          | item, indicator, breadcrumb    |
| form        | Формы                              | label, input-bg, checkbox      |
| avatar      | Аватары                            | bg, text, presence             |
| progress    | Прогресс                           | track, fill                    |
| scrollbar   | Скроллбар                          | track, thumb                   |
| selection   | Выделение                          | bg, text, highlight            |
| **overlay** | **Оверлеи** (новое)                | scrim, backdrop, tint, skeleton|

### Новая категория: Overlay (Оверлеи)

Токены для модальных окон, загрузки, drag & drop.

| Subcategory  | Variant       | Описание                      |
|--------------|---------------|-------------------------------|
| scrim        | default/light/heavy | Затемнение для модалок   |
| backdrop     | default/light/blur  | Фон с размытием          |
| tint         | brand/success/warning/error/info | Цветные оверлеи |
| gradient     | top/bottom/left/right | Градиентные оверлеи    |
| skeleton     | base/shimmer  | Скелетон загрузки             |
| selection    | default/subtle| Выделение элементов           |
| drop-zone    | default/active/valid/invalid | Drag & Drop      |
| drag-preview | default       | Превью перетаскивания         |
| image        | darken/lighten| Оверлеи для изображений       |
| disabled     | default       | Оверлей отключенного состояния|

---

## 3. Components (Компонентные токены)

Токены для конкретных компонентов. **Без режимов** - ссылаются на Tokens.

### Компоненты (~270 токенов)

- Button (primary, secondary, tertiary, ghost, danger, success, warning)
- Input (default, filled состояния)
- Card (default, elevated, interactive)
- Badge (neutral, brand, success, warning, error, info)
- Alert (success, warning, error, info)
- Navigation (item, indicator)
- Modal (overlay, container)
- Tooltip (bg, text, border)
- Table (header, row, border)
- Dropdown
- Breadcrumb
- Chip
- Stepper
- Accordion
- Slider
- Datepicker
- Popover
- Snackbar
- Empty-state
- Upload
- Sidebar

---

## 4. Структура в Figma Variables

```
📁 Primitives (Collection)
   └── Mode: Default
       ├── colors/
       │   ├── neutral/
       │   │   ├── neutral-25
       │   │   ├── neutral-50
       │   │   └── ...neutral-950
       │   ├── success/
       │   ├── warning/
       │   ├── error/
       │   ├── info/
       │   ├── brand/
       │   ├── accent/
       │   └── base/
       │       ├── white
       │       ├── black
       │       ├── transparent-light
       │       └── transparent-dark
       ├── typography/
       ├── spacing/
       ├── radius/
       └── shadows/

📁 Tokens (Collection)
   ├── Mode: light
   │   ├── bg/
   │   ├── text/
   │   ├── border/
   │   ├── icon/
   │   ├── action/
   │   ├── overlay/  ← NEW
   │   └── ...
   └── Mode: dark
       └── ... (инвертированные значения)

📁 Components (Collection)
   └── Mode: Default
       ├── button/
       ├── input/
       ├── card/
       ├── modal/
       │   └── overlay → {Tokens/overlay/scrim/default}
       └── ...
```

---

## 5. Режимы (Modes)

| Collection  | Modes                              |
|-------------|-----------------------------------|
| Primitives  | Default (без режимов)             |
| Tokens      | light, dark, {theme}-light, {theme}-dark |
| Components  | Default (без режимов)             |

### Темы
Темы добавляют дополнительные режимы в Tokens:
- `{theme}-light` - светлая версия темы
- `{theme}-dark` - тёмная версия темы

---

## 6. История изменений

### v1.4.0 (2024-12-16)
- ✅ Brand и Accent вынесены в отдельную секцию (редактируемые)
- ✅ Базовые цвета: white, black, transparent-light (30%), transparent-dark (70%)
- ✅ Системные цвета: neutral, success, warning, error, info (заблокированы)
- ✅ Добавлены Overlay токены (scrim, backdrop, tint, gradient, skeleton, selection, drag & drop)

### v1.3.0
- Добавлены 12 новых компонентов (dropdown, breadcrumb, chip, stepper, accordion, slider, datepicker, popover, snackbar, empty-state, upload, sidebar)
- Настройки перенесены в модальное окно
- Token Manager занимает всю ширину

### v1.2.0
- Трёхуровневая архитектура: Primitives → Tokens → Components
- Режимы light/dark для Tokens
- Компонентные токены без режимов

---

## 7. Использование

### CSS переменные
```css
/* Primitives */
--primitives-neutral-500: #999999;
--primitives-brand-500: #2781F3;
--primitives-base-white: #FFFFFF;
--primitives-base-transparent-light: rgba(0,0,0,0.3);

/* Tokens */
--tokens-bg-page-primary: var(--primitives-neutral-25);
--tokens-text-primary: var(--primitives-neutral-900);
--tokens-overlay-scrim-default: var(--primitives-neutral-900);

/* Components */
--components-button-primary-bg: var(--tokens-action-primary);
--components-modal-overlay: var(--tokens-overlay-scrim-default);
```

### Figma
1. Выбрать слой
2. Применить переменную из нужной коллекции
3. Для смены темы - переключить режим в Tokens

---

*Последнее обновление: 16 декабря 2024*
