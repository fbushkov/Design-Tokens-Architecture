# 🏗️ Архитектура плагина Design Tokens Manager

## 📌 Основная идея

**"Плагин как единственный источник истины"** (Plugin as Source of Truth)

Плагин управляет всеми дизайн-токенами централизованно и синхронизирует их с Figma Variables. Это обеспечивает:
- Единую точку управления токенами
- Консистентность между дизайном и кодом
- Экспорт в различные форматы (CSS, SCSS, JSON, Tailwind)

---

## 🎯 3-уровневая архитектура токенов

```
┌─────────────────────────────────────────────────────────────┐
│                    COMPONENTS (107 токенов)                 │
│        button/primary/primary-bg → action/primary/primary   │
│        input/container/container-stroke → stroke/default    │
└─────────────────────────────────────────────────────────────┘
                              ↓ ссылается на
┌─────────────────────────────────────────────────────────────┐
│                     TOKENS (366 токенов)                    │
│        action/primary/primary → colors/brand/brand-500      │
│        text/primary/primary → colors/neutral/neutral-900    │
│                    (light/dark режимы)                      │
└─────────────────────────────────────────────────────────────┘
                              ↓ ссылается на
┌─────────────────────────────────────────────────────────────┐
│                   PRIMITIVES (355 токенов)                  │
│           colors/brand/brand-500 = #3B82F6                  │
│           colors/neutral/neutral-900 = #1F2937              │
└─────────────────────────────────────────────────────────────┘
```

---

## 📁 Текущая структура модулей

| Папка | Назначение | Статус |
|-------|------------|--------|
| `src/plugin/code.ts` | Основной код плагина (Figma API) | ✅ Активно |
| `src/ui/ui.ts` | Интерфейс плагина | ✅ Активно |
| `src/ui/ui.html` | HTML разметка UI | ✅ Активно |
| `src/generators/` | Генераторы цветовых палитр | 🔄 Частично |
| `src/tokens/` | Примеры токенов (JSON) | 📦 Заготовка |
| `src/types/` | TypeScript типы | 📦 Заготовка |
| `src/utils/` | Утилиты для токенов | 📦 Заготовка |

---

## 🎨 Категории Semantic Tokens (Tokens collection)

| Категория | Описание | Примеры |
|-----------|----------|---------|
| `bg/` | Фоны | page, card, interactive, overlay |
| `text/` | Текст | primary, secondary, link, label |
| `border/` | Границы | default, subtle, focus, error |
| `icon/` | Иконки | primary, secondary, brand |
| `action/` | Интерактивные элементы | primary, secondary, danger |
| `surface/` | Поверхности | page, card, elevated |
| `content/` | Контент (legacy) | primary, secondary |
| `stroke/` | Обводки (legacy) | default, subtle, focus |
| `feedback/` | Статусы | success, warning, error, info |
| `status/` | Индикаторы состояния | online, offline, pending |
| `nav/` | Навигация | item, tab, breadcrumb |
| `form/` | Формы | label, input, checkbox |
| `data-viz/` | Визуализация данных | categorical, sequential |

---

## 🔮 Планируемые модули

| Модуль | Описание | Приоритет |
|--------|----------|-----------|
| **Typography tokens** | Шрифты, размеры, высота строки | 🔴 Высокий |
| **Spacing tokens** | Отступы, gaps | 🔴 Высокий |
| **Effects tokens** | Тени, blur, opacity | 🟡 Средний |
| **Radius tokens** | Border radius | 🟡 Средний |
| **Motion tokens** | Анимации, transitions | 🟢 Низкий |
| **Storybook export** | Экспорт для Storybook | 🟡 Средний |
| **Tokens Studio sync** | Синхронизация с Tokens Studio | 🟢 Низкий |
| **Git sync** | Версионирование токенов | 🟢 Низкий |

---

## 🔄 Поток данных

```
[UI Plugin]                    [Figma Plugin Code]              [Figma]
     │                                │                            │
     │  1. Выбор цветов              │                            │
     │  2. Генерация палитры         │                            │
     │  3. Нажатие "Sync"            │                            │
     │ ─────────────────────────────►│                            │
     │                                │  4. Create Collections    │
     │                                │  5. Create Variables      │
     │                                │ ──────────────────────────►│
     │                                │                            │
     │                                │  6. Set light/dark modes  │
     │                                │ ──────────────────────────►│
     │◄─────────────────────────────── │                            │
     │  7. "Токены созданы!"         │                            │
```

---

## 📝 Именование токенов

### Primitives:
```
colors/{palette}/{palette}-{step}
colors/brand/brand-500
colors/neutral/neutral-100
```

### Tokens (Semantic):
```
{category}/{subcategory}/{subcategory}[-variant][-state]
text/primary/primary
text/primary/primary-hover
text/link/link-default
bg/card/card-primary-hover
```

### Components:
```
{component}/{variant}/{variant}-{property}[-state]
button/primary/primary-bg
button/primary/primary-bg-hover
input/container/container-stroke-focus
```

---

## 🎨 Цветовая шкала Primitives

Шкала от 25 до 975 с шагом 25:
- **25-475**: Светлые оттенки (смешение с белым)
- **500**: Базовый цвет
- **525-975**: Тёмные оттенки (смешение с чёрным)

Палитры по умолчанию:
- `brand` - основной цвет бренда
- `accent` - акцентный цвет
- `neutral` - нейтральные оттенки
- `success` - успех (зелёный)
- `warning` - предупреждение (жёлтый)
- `error` - ошибка (красный)

---

## 🌓 Темы Light/Dark

- **Primitives** - без режимов (только базовые значения)
- **Tokens** - light/dark режимы (ссылки на разные primitives)
- **Components** - light/dark режимы (наследуют от Tokens)

---

## 📅 История изменений

- **2025-12-16**: Исправлено именование токенов (согласованность папок и переменных)
- **2025-12-15**: Добавлен префикс цвета в primitives (brand-500 вместо 500)
- **2025-12-15**: Базовая структура 3-уровневой архитектуры
